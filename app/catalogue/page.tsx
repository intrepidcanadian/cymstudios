import GiftCardCatalog from '@/components/catalogue/GiftCardCatalog';

export const metadata = {
  title: 'Gift Card Catalogue - CYM Studio',
  description: 'Browse and purchase gift cards from 600+ brands with USDC on Ethereum.',
};

export default function CataloguePage() {
  return <GiftCardCatalog />;
}
