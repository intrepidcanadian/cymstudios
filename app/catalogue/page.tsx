import GiftCardCatalog from '@/components/catalogue/GiftCardCatalog';

export const metadata = {
  title: 'Loyalty Reward Center - CYM Studio',
  description: 'Browse and redeem loyalty rewards from 600+ brands with digital USDC tokens.',
};

export default function CataloguePage() {
  return <GiftCardCatalog />;
}
